1) clone the repository: git clone https://github.com/ealouane/hello-cli
				                 cd hello-cli

2) npm install -g : will install the node package globally

3) type "hello" into your CMD Prompt
